1.
SELECT*FROM PRICES
WHERE product_id IN (1,2,3) ;

2.
SELECT PRODUCT_NAME FROM PRICES
WHERE PRICE=(SELECT MAX(price)FROM PRICES);

3.
SELECT SUM (QUANTITY), TYPE FROM STOCK GROUP BY TYPE; 


4.
SELECT SUM (QUANTITY)TYPE, FROM STOCK GROUP BY TYPE
HAVING SUM(QUANTITY)TYPE> 5;


5.

SELECT COUNT(PRODUCT_NAME)
FROM PRICES INNER JOIN STOCK ON PRACE.PRODUCT_ID=STOCK.PRODUCT_ID  HAVING SUM(QUANTITY)>10;

